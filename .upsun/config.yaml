applications:
c-users-glc-platform-test:
type: "nodejs:24"

```
web:
  commands:
    start: "node index.js"

  locations:
    "/":
      passthru: true

hooks:
  build: |
    npm install
```

routes:
"https://{default}/":
type: upstream
upstream: "c-users-glc-platform-test:http"
