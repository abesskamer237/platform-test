```yaml
applications:
  app:
    type: "nodejs:20"

    source:
      root: "/"

    web:
      commands:
        start: "node index.js"

routes:
  "https://{default}/":
    type: upstream
    upstream: "app:http"
```
