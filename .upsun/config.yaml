
---

# Ton fichier doit devenir EXACTEMENT :

:::writing{variant="standard" id="51248"}
applications:
  c-users-glc-platform-test:
    type: "nodejs:24"

    web:
      commands:
        start: "node index.js"

      locations:
        "/":
          passthru: true

    hooks:
      build: |
        npm install

routes:
  "https://{default}/":
    type: upstream
    upstream: "c-users-glc-platform-test:http"
:::

---

# Ensuite

```powershell id="a4"
git add .
git commit -m "Remove broken markdown"
git push origin main